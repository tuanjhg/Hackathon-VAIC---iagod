import { RecommendationCard } from "@/components/RecommendationCard";
import type { ChatEntry } from "@/stores/chat-store";
export function ChatMessage({ message, onQuickReply }: { message: ChatEntry; onQuickReply: (value: string) => void }) { const assistant = message.role === "assistant"; return <div className={`flex ${assistant ? "justify-start" : "justify-end"}`}><div className={`max-w-[88%] ${assistant ? "" : "rounded-2xl rounded-br-sm bg-brand-600 px-3 py-2 text-sm text-white"}`}>{assistant && <p className="rounded-2xl rounded-bl-sm bg-slate-100 px-3 py-2 text-sm leading-5">{message.content}</p>}{message.quickReplies && <div className="mt-2 flex flex-wrap gap-1.5">{message.quickReplies.map((reply) => <button key={reply} onClick={() => onQuickReply(reply)} className="rounded-full border border-brand-300 bg-white px-2.5 py-1 text-xs font-semibold text-brand-700 hover:bg-brand-50">{reply}</button>)}</div>}{message.recommendations && <div className="mt-2 grid gap-2">{message.recommendations.map((item) => <RecommendationCard key={item.product.id} item={item}/>)}</div>}</div></div>; }

